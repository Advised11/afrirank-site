import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Update this to your real production domain before building for SEO/canonical URLs.
  site: 'https://afrirank.com',
  compressHTML: true,
});
